[pytest]
norecursedirs=
    .pytest_cache
    .venv
    data
    secrets
    videos
addopts=
    --cov transcribe_aws
    --cov-report=html
    --cov-report=term-missing
    --cov-config .coveragerc
    --durations=10